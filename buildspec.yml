version: 0.2

phases:
  install:
    commands:
      - echo "Installing dependencies..."
  build:
    commands:
      - echo "Building the project..."
      - mvn clean package

artifacts:
  files:
    - target/*.jar
  base-directory: target
